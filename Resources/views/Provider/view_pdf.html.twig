{% set options = options|merge({'src': options.src ~ '#view=Fit'}) %}
<embed {% for name, value in options %}{{ name }}="{{ value }}" {% endfor %} pluginspage="http://www.adobe.com/products/acrobat/readstep2.html">
